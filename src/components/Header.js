import SearchForm from "./SearchForm";

function Header() {
  return (
    <header className="App-header">
      <h1>iTuneSearch</h1>
      <SearchForm />
    </header>
  );
}

export default Header;
